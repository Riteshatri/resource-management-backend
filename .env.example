# ===========================================
# 🔧 BACKEND ENVIRONMENT CONFIGURATION
# ===========================================
# 📌 DUAL SUPPORT: This app supports TWO ways to load environment variables:
#
#   1️⃣  .env FILE (VM Deployment)
#       - Copy this file to .env in backend/ folder
#       - Fill in your actual values
#       - Perfect for Azure VM deployment
#
#   2️⃣  ENVIRONMENT VARIABLES ( Secrets)
#       - Set these as environment variables
#
# 🎯 Priority: .env file > Environment Variables > Default values
# ===========================================

# ============ Application Environment ============
# Options: development, production
# Development: localhost + auto-reload
# Production: 0.0.0.0 + multiple workers
APP_ENV=production

# ============ Server Configuration ============
PORT=8000
UVICORN_WORKERS=4

# ============ Azure SQL Database ============
# Required for production deployment with Azure SQL
# Leave empty to use SQLite (automatic fallback)
AZURE_SQL_SERVER=ritserver.database.windows.net
AZURE_SQL_DATABASE=ritserver
AZURE_SQL_USERNAME=ritserver
AZURE_SQL_PASSWORD=Ritesh@12345

# Example (CHANGE ACCORDINGLY):
# AZURE_SQL_SERVER=ritserver.database.windows.net
# AZURE_SQL_DATABASE=resource-dashboard
# AZURE_SQL_USERNAME=ritserver
# AZURE_SQL_PASSWORD=YourSecurePassword@123

# ============ Security ============
# Generate a strong random string (minimum 32 characters)
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -hex 32
SECRET_KEY=1f7abb32c57632c35cbf57657f20ca104d88e18dd3cb17050649b10664cd743f

# JWT token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============ CORS Configuration ============
# Comma-separated list of allowed origins
# Development: * (allow all)
# Production: http://frontend-ip:5000,https://yourdomain.com
CORS_ALLOW_ORIGINS=*

# Frontend URL for reference
FRONTEND_URL=http://98.71.91.160:5000

# Example for Azure VM (CHANGE ACCORDINGLY):
# CORS_ALLOW_ORIGINS=http://98.71.75.6:5000,https://mydomain.com
# FRONTEND_URL=http://98.71.75.6:5000

# ============ Optional Settings ============
# Uncomment and modify as needed

# Log level
# LOG_LEVEL=INFO

# Database connection pool settings
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# Enable/disable auto schema creation on startup
# AUTO_CREATE_TABLES=true
